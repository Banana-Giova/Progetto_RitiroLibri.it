module.exports = {
    content: [
      "./src/**/*.{js,jsx,ts,tsx}",
    ],
    theme: {
      extend: {
        colors: {
          'whatsapp-green': '#25D366', // Colore CTA
          'brand-orange': '#FF6B35',   // Colore secondario (logo)
        }
      },
    },
    plugins: [],
  }